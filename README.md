# Convert-ZTAssessmentResultsToJson
A PowerShell script that parses Zero Trust Assessment HTML test results and converts them into a structured JSON file grouped by pillar, suitable for import into the Zero Trust Assessment tool.
